First Register Loads
    LUI
    ADDI

Then Math Operations
    All ARITH_REG
    All ARITH_IMM
    All ARITH32_REG & IMM
        Addiw
        slliw
        srliw
        sraiw
        lui (upgrade)
        auipc (upgrade)
        addw
        sllw
        srlw
        subw
        sraw

Then Jump Operations
    JAL
    JALR

Then Branch Operations
Then Environment Operations
Then Load/Store Operations
Then Implement CSRs
